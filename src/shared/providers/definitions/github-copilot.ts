// Temporary stub: upstream provider definition not yet merged into this working copy.
// Presence required because src/shared/providers/index.ts side-effect-imports it.
// TODO(baseline): restore real provider definition or remove the import line.
export {}
